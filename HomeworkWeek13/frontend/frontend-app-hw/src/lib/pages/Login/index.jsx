import { Grid } from "@chakra-ui/react";
import { LoginComponent } from "./components";

const Login = () => {
  return (
    <Grid gap={2}>
      <LoginComponent />
    </Grid>
  );
};

export default Login;
