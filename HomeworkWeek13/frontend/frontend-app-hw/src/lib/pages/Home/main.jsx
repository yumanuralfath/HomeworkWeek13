import { Grid } from "@chakra-ui/react";
import SomeText from "./components/someText";

const Home = () => {
  return (
    <Grid gap={2}>
      <SomeText />
    </Grid>
  );
};

export default Home;
